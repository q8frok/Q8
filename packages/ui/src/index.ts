/**
 * @q8/ui - Q8 Design System
 *
 * This package provides design tokens, Tailwind configuration,
 * and shared UI primitives for the Q8 dashboard.
 *
 * Design Philosophy:
 * "Glass is a surface, not the content itself.
 *  Neon is an accent, not a background."
 */

export * from './tokens';
